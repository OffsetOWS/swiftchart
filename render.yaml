services:
  - type: web
    name: swiftchart-bot
    runtime: python
    plan: free
    buildCommand: pip install -r bot/requirements.txt
    startCommand: uvicorn bot.webhook:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: TELEGRAM_WEBHOOK_SECRET
        sync: false
      - key: DEFAULT_EXCHANGE
        value: hyperliquid
      - key: DEFAULT_TIMEFRAME
        value: 4h
      - key: LIVE_TRADING_ENABLED
        value: "false"
      - key: ALERTS_ENABLED
        value: "true"
      - key: ALERT_EXCHANGE
        value: hyperliquid
      - key: ALERT_TIMEFRAME
        value: 4h
      - key: ALERT_SCAN_INTERVAL_SECONDS
        value: "1800"
      - key: ALERTS_RUN_SECRET
        sync: false
